<%= form_with(model: faqpost, local: true) do |form| %>
  <% if faqpost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(faqpost.errors.count, "error") %> prohibited this faqpost from being saved:</h2>

      <ul>
      <% faqpost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.collection_select(:faqcat_id, Faqcat.all, :id, :title, {prompt: "Choose a category" }) %><br>
  </div> 

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :faqpost_title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :faqpost_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
